<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-2">
     <%= render 'tag_list', tag_list: @tag_list %>
    </div>
     <div class="col-md-10 p-3">
      <h1 class="main-title mb-2"><%= @tag.name %>(<%= @posts.total_count %>)</h1>

     <%= render 'index', posts: @posts %>

     <%= paginate @posts %>
  </div>
 </div>
</div>
