<div class="container">
  <div class="row justify-content-center">
   <h1 class="main-title mb-2">Drafts (<%= @posts.total_count %>)</h1>
  </div>
  <div class="col m-3">
    
   <%= render 'index', posts: @posts %>

   <%= paginate @posts %>

  </div>
 </div>
</div>