<div class="container">
 <div class="border-bottom border-info" style="padding:10px;">
  <div class="row justify-content-center">
   <h2 class="title mb-2">Comments</h2>
  </div>
 </div>

 <% if @comments.present? %>
  <div class="row justify-content-center">
   <div class="col-10 col-lg-8">
    <table class="table table-hover table-inverse m-4">
    <thead>
	    <tr>
	     <th>ID</th>
	     <th>Name</th>
	     <th>Comment</th>
	     <th></th>
	    </tr>
    </thead>
    <tbody>
    <% @comments.each do |comment| %>
     <tr>
      <td><%= comment.user_id %></td>
      <td><%= comment.user.name %></td>
      <td><%= comment.content %></td>
      <td><%= link_to "Show", admin_post_path(comment.post_id), class:"btn btn-primary" %></td>
     </tr>
    </tbody>
    <% end %>
   </table>
  </div>
 </div>

 <% else %>

 投稿しているコメントはありません

 <% end %>

</div>


